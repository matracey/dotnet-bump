name: Release
on:
  push:
    tags:
      - "**"
jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Hello world
        run: echo "Hello world"
